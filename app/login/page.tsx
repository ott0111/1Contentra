'use client';import Link from"next/link";import{useState}from"react";import{Sparkles,ArrowRight}from"lucide-react";import{Button}from"@/components/ui";export default function Login(){const[email,setEmail]=useState(""),[mode,setMode]=useState<"login"|"signup">("login");function submit(){window.location.href="/onboarding"}return <main style={{minHeight:"100vh",display:"grid",placeItems:"center",padding:20,background:"radial-gradient(circle at 50% 0%,#ff6f0020,transparent 38%),#0b0b0d"}}><div className="card section" style={{width:"min(430px,100%)"}}><div style={{display:"flex",alignItems:"center",gap:9,marginBottom:28}}><span className="logo"/><b>Contentra</b></div><div className="eyebrow">{mode==="login"?"Welcome back":"Start growing"}</div><h1 className="h1">{mode==="login"?"Sign in to Contentra":"Create your workspace"}</h1><p className="sub">{mode==="login"?"Pick up where you left off.":"Your brand, content and growth system in one place."}</p><div className="field"><label>Email</label><input className="input" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></div><div className="field"><label>Password</label><input className="input" type="password" placeholder="••••••••"/></div><Button variant="primary" onClick={submit}>{mode==="login"?"Sign in":"Create account"} <ArrowRight size={14}/></Button><p className="sub" style={{textAlign:"center",marginTop:18}}>{mode==="login"?"New to Contentra?":"Already have an account?"} <button className="btn ghost" onClick={()=>setMode(mode==="login"?"signup":"login")}>{mode==="login"?"Sign up":"Sign in"}</button></p><Link href="/home" className="muted" style={{fontSize:11}}>Continue in demo mode</Link></div></main>}