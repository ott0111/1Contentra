'use client';import{useState}from"react";import{Plus,Play,Pause,Trash2}from"lucide-react";import{Card,Button}from"@/components/ui";export default function Automations(){const[items,setItems]=useState([{name:"Weekly Content Engine",steps:["Every Monday","Find trending content","Generate 10 concepts","Send to Creatos"],on:true},{name:"Repurpose Winners",steps:["When content beats average","Create 3 remixes","Add to approval queue"],on:false},{name:"Trend Hunter",steps:["Every morning","Analyze trends","Notify me"],on:true}]);return <><div className="section-head"><div><div className="eyebrow">Workflow engine</div><h1 className="h1">Automations</h1><p className="sub">Turn repeatable growth work into systems.</p></div><Button variant="primary" onClick={()=>setItems(v=>[{name:"New automation",steps:["Trigger","Action","Action"],on:false},...v])}><Plus size={14}/> Create automation</Button></div><div className="grid three">{items.map((a,i)=><Card className="section" key={i}><div className="section-head"><b>{a.name}</b><button className="iconbtn" onClick={()=>setItems(v=>v.map((x,j)=>j===i?{...x,on:!x.on}:x))}>{a.on?<Pause size={14}/>:<Play size={14}/>}</button></div>{a.steps.map((s,j)=><div key={s} style={{padding:"9px 10px",marginTop:6,border:"1px solid rgba(255,255,255,.06)",borderRadius:9,fontSize:11}}>{j+1}. {s}</div>)}<div style={{display:"flex",justifyContent:"space-between",marginTop:14}}><span className={a.on?"status":"pill"}>{a.on?"Enabled":"Disabled"}</span><button className="iconbtn" onClick={()=>setItems(v=>v.filter((_,j)=>j!==i))}><Trash2 size={14}/></button></div></Card>)}</div></>}