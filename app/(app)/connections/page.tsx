'use client';import{useState}from"react";import{Link2,CheckCircle2}from"lucide-react";import{Card,Button}from"@/components/ui";export default function Connections(){const[connected,setConnected]=useState<string[]>(["Instagram"]);const platforms=["TikTok","Instagram","YouTube","LinkedIn","X"];return <><div className="eyebrow">Distribution</div><h1 className="h1">Connections</h1><p className="sub">Provider abstraction with demo mode keeps the workflow usable until OAuth credentials are configured.</p><div className="grid three" style={{marginTop:20}}>{platforms.map(p=><Card className="section" key={p}><Link2 size={18} color="#ff7a18"/><h3>{p}</h3><p className="sub">{connected.includes(p)?"Connected · Demo account":"Not connected"}</p><Button variant={connected.includes(p)?"ghost":"primary"} onClick={()=>setConnected(v=>v.includes(p)?v.filter(x=>x!==p):[...v,p])}>{connected.includes(p)?<><CheckCircle2 size={14}/> Connected</>:"Connect account"}</Button></Card>)}</div></>}