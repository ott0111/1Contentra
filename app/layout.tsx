import "./globals.css";import {ContentraProvider} from "@/components/contentra-provider";export const metadata={title:"Contentra — The Operating System for Creators",description:"Connect your business, see what matters, and know what to do next."};export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body><ContentraProvider>{children}</ContentraProvider></body></html>}