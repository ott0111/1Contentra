export type ProviderMode="demo"|"live";export interface ContentGenerationProvider{generate(input:{type:string;topic:string;hook:string}):Promise<{title:string;hook:string;script:string;mode:ProviderMode}>}export const demoContentProvider:ContentGenerationProvider={async generate(input){return{title:input.topic||"Generated Content",hook:input.hook||"A strong Contentra hook",script:"Hook → proof → product moment → CTA",mode:"demo"}}};export interface SocialProvider{connect(platform:string):Promise<{platform:string;status:"connected";mode:ProviderMode}>;publish(contentId:string,platform:string):Promise<{contentId:string;platform:string;status:"published";mode:ProviderMode}>}export const demoSocialProvider:SocialProvider={async connect(platform){return{platform,status:"connected",mode:"demo"}},async publish(contentId,platform){return{contentId,platform,status:"published",mode:"demo"}}};